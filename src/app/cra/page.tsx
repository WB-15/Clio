import { mergeMetadataWithDefault } from '@/utils/seo'

export const metadata = mergeMetadataWithDefault({ title: 'Cra' })

const CraPage = () => {
  return <div>Cra page</div>
}

export default CraPage
