import { mergeMetadataWithDefault } from '@/utils/seo'

export const metadata = mergeMetadataWithDefault({ title: 'Patients List' })

const TabPatientsList = () => {
  return <div>Patients list Page</div>
}

export default TabPatientsList
