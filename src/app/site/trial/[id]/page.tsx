import { mergeMetadataWithDefault } from '@/utils/seo'

export const metadata = mergeMetadataWithDefault({ title: 'Trial' })

const TrialPage = () => {
  return <div>Trial Page</div>
}

export default TrialPage
