import { mergeMetadataWithDefault } from '@/utils/seo'

export const metadata = mergeMetadataWithDefault({ title: 'Cra list' })

const TabCraList = () => {
  return <div>Cra list Page</div>
}

export default TabCraList
