import { mergeMetadataWithDefault } from '@/utils/seo'

export const metadata = mergeMetadataWithDefault({ title: 'Patient' })

const PatientPage = () => {
  return <div>Patient page</div>
}

export default PatientPage
